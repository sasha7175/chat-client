services:
  - type: web
    name: phaser-chatroom-server
    env: node
    rootDir: games/10-networked/chat-server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /health

